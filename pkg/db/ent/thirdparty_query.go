// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/third-login-gateway/pkg/db/ent/predicate"
	"github.com/NpoolPlatform/third-login-gateway/pkg/db/ent/thirdparty"
	"github.com/google/uuid"
)

// ThirdPartyQuery is the builder for querying ThirdParty entities.
type ThirdPartyQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.ThirdParty
	modifiers  []func(s *sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ThirdPartyQuery builder.
func (tpq *ThirdPartyQuery) Where(ps ...predicate.ThirdParty) *ThirdPartyQuery {
	tpq.predicates = append(tpq.predicates, ps...)
	return tpq
}

// Limit adds a limit step to the query.
func (tpq *ThirdPartyQuery) Limit(limit int) *ThirdPartyQuery {
	tpq.limit = &limit
	return tpq
}

// Offset adds an offset step to the query.
func (tpq *ThirdPartyQuery) Offset(offset int) *ThirdPartyQuery {
	tpq.offset = &offset
	return tpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tpq *ThirdPartyQuery) Unique(unique bool) *ThirdPartyQuery {
	tpq.unique = &unique
	return tpq
}

// Order adds an order step to the query.
func (tpq *ThirdPartyQuery) Order(o ...OrderFunc) *ThirdPartyQuery {
	tpq.order = append(tpq.order, o...)
	return tpq
}

// First returns the first ThirdParty entity from the query.
// Returns a *NotFoundError when no ThirdParty was found.
func (tpq *ThirdPartyQuery) First(ctx context.Context) (*ThirdParty, error) {
	nodes, err := tpq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{thirdparty.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tpq *ThirdPartyQuery) FirstX(ctx context.Context) *ThirdParty {
	node, err := tpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ThirdParty ID from the query.
// Returns a *NotFoundError when no ThirdParty ID was found.
func (tpq *ThirdPartyQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = tpq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{thirdparty.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tpq *ThirdPartyQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := tpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ThirdParty entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ThirdParty entity is found.
// Returns a *NotFoundError when no ThirdParty entities are found.
func (tpq *ThirdPartyQuery) Only(ctx context.Context) (*ThirdParty, error) {
	nodes, err := tpq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{thirdparty.Label}
	default:
		return nil, &NotSingularError{thirdparty.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tpq *ThirdPartyQuery) OnlyX(ctx context.Context) *ThirdParty {
	node, err := tpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ThirdParty ID in the query.
// Returns a *NotSingularError when more than one ThirdParty ID is found.
// Returns a *NotFoundError when no entities are found.
func (tpq *ThirdPartyQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = tpq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{thirdparty.Label}
	default:
		err = &NotSingularError{thirdparty.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tpq *ThirdPartyQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := tpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ThirdParties.
func (tpq *ThirdPartyQuery) All(ctx context.Context) ([]*ThirdParty, error) {
	if err := tpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return tpq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (tpq *ThirdPartyQuery) AllX(ctx context.Context) []*ThirdParty {
	nodes, err := tpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ThirdParty IDs.
func (tpq *ThirdPartyQuery) IDs(ctx context.Context) ([]uuid.UUID, error) {
	var ids []uuid.UUID
	if err := tpq.Select(thirdparty.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tpq *ThirdPartyQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := tpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tpq *ThirdPartyQuery) Count(ctx context.Context) (int, error) {
	if err := tpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return tpq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (tpq *ThirdPartyQuery) CountX(ctx context.Context) int {
	count, err := tpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tpq *ThirdPartyQuery) Exist(ctx context.Context) (bool, error) {
	if err := tpq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return tpq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (tpq *ThirdPartyQuery) ExistX(ctx context.Context) bool {
	exist, err := tpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ThirdPartyQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tpq *ThirdPartyQuery) Clone() *ThirdPartyQuery {
	if tpq == nil {
		return nil
	}
	return &ThirdPartyQuery{
		config:     tpq.config,
		limit:      tpq.limit,
		offset:     tpq.offset,
		order:      append([]OrderFunc{}, tpq.order...),
		predicates: append([]predicate.ThirdParty{}, tpq.predicates...),
		// clone intermediate query.
		sql:    tpq.sql.Clone(),
		path:   tpq.path,
		unique: tpq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ThirdParty.Query().
//		GroupBy(thirdparty.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (tpq *ThirdPartyQuery) GroupBy(field string, fields ...string) *ThirdPartyGroupBy {
	group := &ThirdPartyGroupBy{config: tpq.config}
	group.fields = append([]string{field}, fields...)
	group.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := tpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return tpq.sqlQuery(ctx), nil
	}
	return group
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.ThirdParty.Query().
//		Select(thirdparty.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (tpq *ThirdPartyQuery) Select(fields ...string) *ThirdPartySelect {
	tpq.fields = append(tpq.fields, fields...)
	return &ThirdPartySelect{ThirdPartyQuery: tpq}
}

func (tpq *ThirdPartyQuery) prepareQuery(ctx context.Context) error {
	for _, f := range tpq.fields {
		if !thirdparty.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tpq.path != nil {
		prev, err := tpq.path(ctx)
		if err != nil {
			return err
		}
		tpq.sql = prev
	}
	if thirdparty.Policy == nil {
		return errors.New("ent: uninitialized thirdparty.Policy (forgotten import ent/runtime?)")
	}
	if err := thirdparty.Policy.EvalQuery(ctx, tpq); err != nil {
		return err
	}
	return nil
}

func (tpq *ThirdPartyQuery) sqlAll(ctx context.Context) ([]*ThirdParty, error) {
	var (
		nodes = []*ThirdParty{}
		_spec = tpq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		node := &ThirdParty{config: tpq.config}
		nodes = append(nodes, node)
		return node.scanValues(columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		if len(nodes) == 0 {
			return fmt.Errorf("ent: Assign called without calling ScanValues")
		}
		node := nodes[len(nodes)-1]
		return node.assignValues(columns, values)
	}
	if len(tpq.modifiers) > 0 {
		_spec.Modifiers = tpq.modifiers
	}
	if err := sqlgraph.QueryNodes(ctx, tpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (tpq *ThirdPartyQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tpq.querySpec()
	if len(tpq.modifiers) > 0 {
		_spec.Modifiers = tpq.modifiers
	}
	_spec.Node.Columns = tpq.fields
	if len(tpq.fields) > 0 {
		_spec.Unique = tpq.unique != nil && *tpq.unique
	}
	return sqlgraph.CountNodes(ctx, tpq.driver, _spec)
}

func (tpq *ThirdPartyQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := tpq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (tpq *ThirdPartyQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   thirdparty.Table,
			Columns: thirdparty.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: thirdparty.FieldID,
			},
		},
		From:   tpq.sql,
		Unique: true,
	}
	if unique := tpq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := tpq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, thirdparty.FieldID)
		for i := range fields {
			if fields[i] != thirdparty.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tpq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tpq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tpq *ThirdPartyQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tpq.driver.Dialect())
	t1 := builder.Table(thirdparty.Table)
	columns := tpq.fields
	if len(columns) == 0 {
		columns = thirdparty.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tpq.sql != nil {
		selector = tpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tpq.unique != nil && *tpq.unique {
		selector.Distinct()
	}
	for _, m := range tpq.modifiers {
		m(selector)
	}
	for _, p := range tpq.predicates {
		p(selector)
	}
	for _, p := range tpq.order {
		p(selector)
	}
	if offset := tpq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tpq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (tpq *ThirdPartyQuery) ForUpdate(opts ...sql.LockOption) *ThirdPartyQuery {
	if tpq.driver.Dialect() == dialect.Postgres {
		tpq.Unique(false)
	}
	tpq.modifiers = append(tpq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return tpq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (tpq *ThirdPartyQuery) ForShare(opts ...sql.LockOption) *ThirdPartyQuery {
	if tpq.driver.Dialect() == dialect.Postgres {
		tpq.Unique(false)
	}
	tpq.modifiers = append(tpq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return tpq
}

// ThirdPartyGroupBy is the group-by builder for ThirdParty entities.
type ThirdPartyGroupBy struct {
	config
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tpgb *ThirdPartyGroupBy) Aggregate(fns ...AggregateFunc) *ThirdPartyGroupBy {
	tpgb.fns = append(tpgb.fns, fns...)
	return tpgb
}

// Scan applies the group-by query and scans the result into the given value.
func (tpgb *ThirdPartyGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := tpgb.path(ctx)
	if err != nil {
		return err
	}
	tpgb.sql = query
	return tpgb.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (tpgb *ThirdPartyGroupBy) ScanX(ctx context.Context, v interface{}) {
	if err := tpgb.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from group-by.
// It is only allowed when executing a group-by query with one field.
func (tpgb *ThirdPartyGroupBy) Strings(ctx context.Context) ([]string, error) {
	if len(tpgb.fields) > 1 {
		return nil, errors.New("ent: ThirdPartyGroupBy.Strings is not achievable when grouping more than 1 field")
	}
	var v []string
	if err := tpgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (tpgb *ThirdPartyGroupBy) StringsX(ctx context.Context) []string {
	v, err := tpgb.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (tpgb *ThirdPartyGroupBy) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = tpgb.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{thirdparty.Label}
	default:
		err = fmt.Errorf("ent: ThirdPartyGroupBy.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (tpgb *ThirdPartyGroupBy) StringX(ctx context.Context) string {
	v, err := tpgb.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from group-by.
// It is only allowed when executing a group-by query with one field.
func (tpgb *ThirdPartyGroupBy) Ints(ctx context.Context) ([]int, error) {
	if len(tpgb.fields) > 1 {
		return nil, errors.New("ent: ThirdPartyGroupBy.Ints is not achievable when grouping more than 1 field")
	}
	var v []int
	if err := tpgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (tpgb *ThirdPartyGroupBy) IntsX(ctx context.Context) []int {
	v, err := tpgb.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (tpgb *ThirdPartyGroupBy) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = tpgb.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{thirdparty.Label}
	default:
		err = fmt.Errorf("ent: ThirdPartyGroupBy.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (tpgb *ThirdPartyGroupBy) IntX(ctx context.Context) int {
	v, err := tpgb.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from group-by.
// It is only allowed when executing a group-by query with one field.
func (tpgb *ThirdPartyGroupBy) Float64s(ctx context.Context) ([]float64, error) {
	if len(tpgb.fields) > 1 {
		return nil, errors.New("ent: ThirdPartyGroupBy.Float64s is not achievable when grouping more than 1 field")
	}
	var v []float64
	if err := tpgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (tpgb *ThirdPartyGroupBy) Float64sX(ctx context.Context) []float64 {
	v, err := tpgb.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (tpgb *ThirdPartyGroupBy) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = tpgb.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{thirdparty.Label}
	default:
		err = fmt.Errorf("ent: ThirdPartyGroupBy.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (tpgb *ThirdPartyGroupBy) Float64X(ctx context.Context) float64 {
	v, err := tpgb.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from group-by.
// It is only allowed when executing a group-by query with one field.
func (tpgb *ThirdPartyGroupBy) Bools(ctx context.Context) ([]bool, error) {
	if len(tpgb.fields) > 1 {
		return nil, errors.New("ent: ThirdPartyGroupBy.Bools is not achievable when grouping more than 1 field")
	}
	var v []bool
	if err := tpgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (tpgb *ThirdPartyGroupBy) BoolsX(ctx context.Context) []bool {
	v, err := tpgb.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a group-by query.
// It is only allowed when executing a group-by query with one field.
func (tpgb *ThirdPartyGroupBy) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = tpgb.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{thirdparty.Label}
	default:
		err = fmt.Errorf("ent: ThirdPartyGroupBy.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (tpgb *ThirdPartyGroupBy) BoolX(ctx context.Context) bool {
	v, err := tpgb.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (tpgb *ThirdPartyGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range tpgb.fields {
		if !thirdparty.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := tpgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tpgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (tpgb *ThirdPartyGroupBy) sqlQuery() *sql.Selector {
	selector := tpgb.sql.Select()
	aggregation := make([]string, 0, len(tpgb.fns))
	for _, fn := range tpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(tpgb.fields)+len(tpgb.fns))
		for _, f := range tpgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(tpgb.fields...)...)
}

// ThirdPartySelect is the builder for selecting fields of ThirdParty entities.
type ThirdPartySelect struct {
	*ThirdPartyQuery
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (tps *ThirdPartySelect) Scan(ctx context.Context, v interface{}) error {
	if err := tps.prepareQuery(ctx); err != nil {
		return err
	}
	tps.sql = tps.ThirdPartyQuery.sqlQuery(ctx)
	return tps.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (tps *ThirdPartySelect) ScanX(ctx context.Context, v interface{}) {
	if err := tps.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from a selector. It is only allowed when selecting one field.
func (tps *ThirdPartySelect) Strings(ctx context.Context) ([]string, error) {
	if len(tps.fields) > 1 {
		return nil, errors.New("ent: ThirdPartySelect.Strings is not achievable when selecting more than 1 field")
	}
	var v []string
	if err := tps.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (tps *ThirdPartySelect) StringsX(ctx context.Context) []string {
	v, err := tps.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from a selector. It is only allowed when selecting one field.
func (tps *ThirdPartySelect) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = tps.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{thirdparty.Label}
	default:
		err = fmt.Errorf("ent: ThirdPartySelect.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (tps *ThirdPartySelect) StringX(ctx context.Context) string {
	v, err := tps.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from a selector. It is only allowed when selecting one field.
func (tps *ThirdPartySelect) Ints(ctx context.Context) ([]int, error) {
	if len(tps.fields) > 1 {
		return nil, errors.New("ent: ThirdPartySelect.Ints is not achievable when selecting more than 1 field")
	}
	var v []int
	if err := tps.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (tps *ThirdPartySelect) IntsX(ctx context.Context) []int {
	v, err := tps.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from a selector. It is only allowed when selecting one field.
func (tps *ThirdPartySelect) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = tps.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{thirdparty.Label}
	default:
		err = fmt.Errorf("ent: ThirdPartySelect.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (tps *ThirdPartySelect) IntX(ctx context.Context) int {
	v, err := tps.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from a selector. It is only allowed when selecting one field.
func (tps *ThirdPartySelect) Float64s(ctx context.Context) ([]float64, error) {
	if len(tps.fields) > 1 {
		return nil, errors.New("ent: ThirdPartySelect.Float64s is not achievable when selecting more than 1 field")
	}
	var v []float64
	if err := tps.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (tps *ThirdPartySelect) Float64sX(ctx context.Context) []float64 {
	v, err := tps.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from a selector. It is only allowed when selecting one field.
func (tps *ThirdPartySelect) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = tps.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{thirdparty.Label}
	default:
		err = fmt.Errorf("ent: ThirdPartySelect.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (tps *ThirdPartySelect) Float64X(ctx context.Context) float64 {
	v, err := tps.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from a selector. It is only allowed when selecting one field.
func (tps *ThirdPartySelect) Bools(ctx context.Context) ([]bool, error) {
	if len(tps.fields) > 1 {
		return nil, errors.New("ent: ThirdPartySelect.Bools is not achievable when selecting more than 1 field")
	}
	var v []bool
	if err := tps.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (tps *ThirdPartySelect) BoolsX(ctx context.Context) []bool {
	v, err := tps.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from a selector. It is only allowed when selecting one field.
func (tps *ThirdPartySelect) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = tps.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{thirdparty.Label}
	default:
		err = fmt.Errorf("ent: ThirdPartySelect.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (tps *ThirdPartySelect) BoolX(ctx context.Context) bool {
	v, err := tps.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (tps *ThirdPartySelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := tps.sql.Query()
	if err := tps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
